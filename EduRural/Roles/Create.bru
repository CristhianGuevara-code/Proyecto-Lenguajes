meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{PATH}}/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "name": "Madre",
    "description": "padre"
  }
}
